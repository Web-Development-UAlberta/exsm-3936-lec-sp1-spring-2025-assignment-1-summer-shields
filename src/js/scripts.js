async function main() {
  //Car class, parameters (make, model, and year), properties (make, model, year, odometer, and engine)
  class Car {
    constructor(make, model, year) {
      this.make = make;
      this.model = model;
      this.year = year;
      this.odometer = 0;
      this.engine = false;
    }

  //class Engine
    //Properties (cylinderCount and isRunning)

    //getters and setters

    //method 1 
    startEngine() {
      engine = true;
    }

    //method 2
    stopEngine() {
      engine = false;
    }

    //method 3
    drive(int) {

    }
  }

  //Instantiate

  //JSON
}
